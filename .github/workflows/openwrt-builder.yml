name: OpenWrt Builder 23.05.6 Core Compiler

on:
  workflow_dispatch:

env:
  REPO_URL: https://github.com/openwrt/openwrt
  REPO_BRANCH: v23.05.6
  FEEDS_CONF: feeds.conf.default
  CONFIG_FILE: .config
  DIY_P1_SH: diy-part1.sh
  DIY_P2_SH: diy-part2.sh
  UPLOAD_BIN_DIR: false
  UPLOAD_FIRMWARE: true
  UPLOAD_RELEASE: true
  TZ: Asia/Bangkok

jobs:
  build:
    runs-on: ubuntu-22.04

    steps:
    - name: Checkout
      uses: actions/checkout@v4

    - name: Initialization environment
      env:
        DEBIAN_FRONTEND: noninteractive
      run: |
        sudo rm -rf /etc/apt/sources.list.d/* /usr/share/dotnet /usr/local/lib/android /opt/ghc /opt/hostedtoolcache/CodeQL
        sudo docker image prune --all --force
        sudo -E apt-get -qq update
        sudo -E apt-get -qq install ack antlr3 asciidoc autoconf automake autopoint binutils bison build-essential bzip2 ccache cmake cpio curl device-tree-compiler fastjar flex gawk gettext gcc-multilib g++-multilib git gperf haveged help2man intltool libc6-dev-i386 libelf-dev libfuse-dev libglib2.0-dev libgmp3-dev libltdl-dev libmpc-dev libmpfr-dev libncurses5-dev libncursesw5-dev libpython3-dev libreadline-dev libssl-dev libtool lrzsz mkisofs msmtp ninja-build p7zip p7zip-full patch pkgconf python2.7 python3 python3-pyelftools python3-setuptools qemu-utils rsync scons squashfs-tools subversion swig texinfo uglifyjs upx-ucl unzip vim wget xmlto xxd zlib1g-dev
        sudo -E apt-get -qq autoremove --purge
        sudo -E apt-get -qq clean
        sudo timedatectl set-timezone "$TZ"
        sudo mkdir -p /workdir
        sudo chown $USER:$GROUPS /workdir

    - name: Clone source code
      working-directory: /workdir
      run: |
        df -hT $PWD
        rm -rf openwrt
        git clone $REPO_URL -b $REPO_BRANCH openwrt
        ln -sf /workdir/openwrt $GITHUB_WORKSPACE/openwrt

    - name: Apply Custom Device Patch
      run: |
        cd openwrt
        patch -p1 < $GITHUB_WORKSPACE/patches/999-fixed2.patch

    - name: Fix DTS syntax for OpenWrt 23.05
      run: |
        cd openwrt
        sed -i 's/Gang_ACTIVE_LOW/GPIO_ACTIVE_LOW/g' target/linux/ramips/dts/mt7628an_cudy_lt300-v3.dts

    - name: Load custom feeds
      run: |
        [ -e $FEEDS_CONF ] && mv $FEEDS_CONF openwrt/feeds.conf.default
        chmod +x $DIY_P1_SH
        cd openwrt
        $GITHUB_WORKSPACE/$DIY_P1_SH

    - name: Update feeds
      run: cd openwrt && ./scripts/feeds update -a

    - name: Install feeds
      run: cd openwrt && ./scripts/feeds install -a

    - name: Configure Target Device
      run: |
        cd openwrt
        
        # 1. ЖЕСТКИЙ ХАК: Полностью отключаем удаление файлов при ложной проверке 'too big'
        # Заменяем команду удаления файла "rm -f $@" на безопасное эхо, чтобы sysupgrade.bin сохранился
        sed -i 's/rm -f \$@/@echo "Пул лимита проигнорирован"/g' include/image.mk
        
        # 2. Пишем конфигурационный файл .config под ваше устройство
        echo 'CONFIG_TARGET_ramips=y' > .config
        echo 'CONFIG_TARGET_ramips_mt76x8=y' >> .config
        echo 'CONFIG_TARGET_ramips_mt76x8_DEVICE_cudy_lt300-v3=y' >> .config
        echo 'CONFIG_TARGET_MULTI_PROFILE=n' >> .config
        
        # Драйверы для 4G-модема MeigLink
        echo 'CONFIG_PACKAGE_luci=y' >> .config
        echo 'CONFIG_PACKAGE_kmod-usb2=y' >> .config
        echo 'CONFIG_PACKAGE_kmod-usb-ohci=y' >> .config
        echo 'CONFIG_PACKAGE_kmod-usb-net-cdc-ether=y' >> .config
        echo 'CONFIG_PACKAGE_kmod-usb-serial-option=y' >> .config
        echo 'CONFIG_PACKAGE_usb-modeswitch=y' >> .config

    - name: Download package
      id: package
      run: |
        cd openwrt
        make defconfig
        make download -j8

    - name: Compile the firmware
      id: compile
      run: |
        cd openwrt
        echo -e "$(nproc) thread compile"
        make -j$(nproc) || make -j1 V=s
        echo "status=success" >> $GITHUB_OUTPUT

    - name: Upload firmware directory
      uses: actions/upload-artifact@v4
      if: steps.compile.outputs.status == 'success' && !cancelled()
      with:
        name: OpenWrt_23.05.6_Cudy_LT300v3_CoreCompiled
        path: openwrt/bin/targets/ramips/mt76x8/*
